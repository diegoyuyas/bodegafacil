# @kduma-autoid/capacitor-bluetooth-printer (impresión Bluetooth) está
# publicado como peer de Capacitor 6.x; este proyecto usa Capacitor 8.
# Sin esto, "npm install" falla con un error de peer dependencies.
legacy-peer-deps=true
